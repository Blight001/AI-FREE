diff --git a/chrome/browser/ui/views/frame/ai_free_page_automation.cc b/chrome/browser/ui/views/frame/ai_free_page_automation.cc
--- a/chrome/browser/ui/views/frame/ai_free_page_automation.cc
+++ b/chrome/browser/ui/views/frame/ai_free_page_automation.cc
@@ -70,6 +70,13 @@ std::string ObserveScript(const base::DictValue& command) {
                        CommandJson(command), R"JS(;
     const limit=Math.max(1,Math.min(1000,Number(a.limit)||200));
     const textLimit=Math.max(20,Math.min(500,Number(a.textLimit)||120));
+    const observeMode=String(a.mode||'elements');
+    const regionMode=String(a.regionMode||'centerInside');
+    const regionPadding=Math.max(0,Math.min(100,Number(a.regionPadding)||0));
+    const includeAncestorContext=Math.max(0,Math.min(6,Number(a.includeAncestorContext)||2));
+    const requestedRegionRef=String(a.regionRef||'');
+    const requestedRegion=a.region&&typeof a.region==='object'?a.region:{};
+    const maxRegionDepth=Math.max(1,Math.min(6,Number(a.maxDepth)||2));
     const keyword=String(a.keyword||'').toLowerCase();
     const list=value=>(Array.isArray(value)?value:[]).map(item=>String(item||'').trim().toLowerCase()).filter(Boolean);
     const tagFilters=[...new Set([...list(a.tags),String(a.tag||'').toLowerCase()].filter(Boolean))];
@@ -101,6 +107,16 @@ std::string ObserveScript(const base::DictValue& command) {
     const selectorInfo=(el)=>{let current=el,parts=[];for(let depth=0;current&&depth<8;depth++,current=current.parentElement){parts.unshift(localSelector(current));const candidate=parts.join(' > ');let count=0;try{count=el.ownerDocument.querySelectorAll(candidate).length;}catch(_){}if(count===1)return {selector:candidate,unique:true,matchCount:1,stability:candidate.startsWith('#')?'high':'medium'};}const selector=parts.join(' > ')||localSelector(el);let count=0;try{count=el.ownerDocument.querySelectorAll(selector).length;}catch(_){}return {selector,unique:count===1,matchCount:count,stability:'low'};};
     const stableRef=(value)=>{let hash=2166136261;for(const char of value){hash^=char.charCodeAt(0);hash=Math.imul(hash,16777619);}return 'node-'+(hash>>>0).toString(16).padStart(8,'0');};
     const containerInfo=(el)=>{const node=el.closest?.('[role="listitem"],li,article,tr,form,[data-testid]');if(!node||node===el)return null;const locator=selectorInfo(node);return {stableRef:stableRef(locator.selector),role:roleOf(node)||({LI:'listitem',TR:'row',FORM:'form',ARTICLE:'article'}[node.tagName]||'group'),selector:locator.selector,text:String(node.innerText||node.textContent||'').trim().replace(/\s+/g,' ').slice(0,textLimit)};};
+    const regionRole=el=>roleOf(el)||({NAV:'navigation',MAIN:'main',ASIDE:'complementary',HEADER:'banner',FOOTER:'contentinfo',FORM:'form',DIALOG:'dialog'}[el.tagName]||'region');
+    const regionLabel=el=>accessibleName(el,labelOf(el)).text||String(el.getAttribute('aria-label')||el.getAttribute('data-testid')||'').trim()||String(el.innerText||'').trim().replace(/\s+/g,' ').slice(0,80);
+    const regionDescriptor=el=>{const r=el.getBoundingClientRect(),locator=selectorInfo(el),layoutHash=stableRef([locator.selector,Math.round(r.left),Math.round(r.top),Math.round(r.width),Math.round(r.height)].join('|'));const c=style(el);return {ref:'region-'+stableRef(locator.selector).slice(5),stableRef:stableRef(locator.selector),role:regionRole(el),label:regionLabel(el),selector:locator.selector,bounds:{x:Math.round(r.left),y:Math.round(r.top),width:Math.round(r.width),height:Math.round(r.height)},coordinateSystem:'viewport-css-px',scrollable:/(auto|scroll)/.test(c.overflow+c.overflowX+c.overflowY),sticky:['fixed','sticky'].includes(c.position),layoutHash};};
+    const regionElements=()=>[...document.querySelectorAll('main,nav,aside,header,footer,form,dialog,[role="main"],[role="navigation"],[role="region"],[role="dialog"],[aria-modal="true"],[data-testid]')].filter(el=>{const r=el.getBoundingClientRect(),depth=[...el.ownerDocument.querySelectorAll('main,nav,aside,header,footer,form,dialog,[role="main"],[role="navigation"],[role="region"],[role="dialog"],[aria-modal="true"],[data-testid]')].filter(parent=>parent!==el&&parent.contains(el)).length+1;return depth<=maxRegionDepth&&rendered(el)&&r.width>40&&r.height>30&&r.bottom>0&&r.right>0&&r.top<innerHeight&&r.left<innerWidth;});
+    const regions=()=>regionElements().map(regionDescriptor);
+    const semanticRegion=()=>{const role=String(requestedRegion.role||'').toLowerCase(),label=String(requestedRegion.label||'').toLowerCase();const candidates=regionElements().filter(el=>(!role||regionRole(el)===role)&&(!label||regionLabel(el).toLowerCase().includes(label)));return candidates[0]||null;};
+    const selectedRegionElement=()=>requestedRegionRef?regionElements().find(el=>regionDescriptor(el).ref===requestedRegionRef)||null:semanticRegion();
+    const rectangleRegion=()=>Number.isFinite(Number(requestedRegion.x))?{left:Number(requestedRegion.x),top:Number(requestedRegion.y),right:Number(requestedRegion.x)+Number(requestedRegion.width),bottom:Number(requestedRegion.y)+Number(requestedRegion.height),width:Number(requestedRegion.width),height:Number(requestedRegion.height)}:null;
+    const intersection=(r,area)=>{const left=Math.max(r.left,area.left-regionPadding),top=Math.max(r.top,area.top-regionPadding),right=Math.min(r.right,area.right+regionPadding),bottom=Math.min(r.bottom,area.bottom+regionPadding),overlap=Math.max(0,right-left)*Math.max(0,bottom-top);return {ratio:Math.round(overlap/Math.max(1,r.width*r.height)*1000)/1000,centerInside:r.left+r.width/2>=area.left-regionPadding&&r.left+r.width/2<=area.right+regionPadding&&r.top+r.height/2>=area.top-regionPadding&&r.top+r.height/2<=area.bottom+regionPadding,fullyInside:r.left>=area.left-regionPadding&&r.right<=area.right+regionPadding&&r.top>=area.top-regionPadding&&r.bottom<=area.bottom+regionPadding};};
+    const ancestorContext=(el,stop)=>{const out=[];for(let current=el.parentElement;current&&current!==stop&&out.length<includeAncestorContext;current=current.parentElement){const role=roleOf(current);const label=regionLabel(current);if(role||label)out.push({role:role||'group',label:label.slice(0,120)});}if(stop&&out.length<includeAncestorContext)out.push({role:regionRole(stop),label:regionLabel(stop).slice(0,120)});return out;};
     const composedParent=(el)=>{if(el.parentElement)return el.parentElement;const root=el.getRootNode?.();return root&&root.host||null;};
     const rendered=(el)=>{for(let current=el;isElement(current);current=composedParent(current)){const c=style(current);if(c.display==='none'||c.visibility==='hidden'||c.visibility==='collapse'||Number(c.opacity)===0||current.inert)return false;}return true;};
     const visible=(el,r)=>rendered(el)&&r.width>=1&&r.height>=1&&r.bottom>0&&r.right>0&&r.top<innerHeight&&r.left<innerWidth;
@@ -113,6 +129,15 @@ std::string ObserveScript(const base::DictValue& command) {
     const composedContains=(owner,hit)=>{for(let current=hit;isElement(current);current=composedParent(current)){if(current===owner)return true;}return false;};
     const candidatePoints=(r)=>{const left=Math.max(0,r.left),top=Math.max(0,r.top),right=Math.min(innerWidth,r.right),bottom=Math.min(innerHeight,r.bottom);if(right-left<1||bottom-top<1)return [];const insetX=Math.min(6,(right-left)/4),insetY=Math.min(6,(bottom-top)/4);return [[(left+right)/2,(top+bottom)/2],[left+insetX,top+insetY],[right-insetX,top+insetY],[left+insetX,bottom-insetY],[right-insetX,bottom-insetY]];};
     const exposedPoint=(el,r,clickable,media)=>{if(clickable&&(el.disabled||el.getAttribute('aria-disabled')==='true'||style(el).pointerEvents==='none'))return null;for(const [x,y] of candidatePoints(r)){const hit=deepElementFromPoint(x,y),owner=clickOwner(hit);if(clickable?(owner===el||(media&&composedContains(el,hit))):!owner&&composedContains(el,hit))return {x,y};}return null;};
+    const regionElement=selectedRegionElement(),regionRect=regionElement?regionElement.getBoundingClientRect():rectangleRegion();
+    if(requestedRegionRef&&!regionElement)return {success:false,errorCode:'REGION_STALE',error:'区域引用已失效，请重新执行 mode=overview',regionRef:requestedRegionRef,regionStillValid:false};
+    const regionData=regionElement?regionDescriptor(regionElement):(regionRect?{ref:'region-rect',role:'region',label:'自定义区域',bounds:{x:regionRect.left,y:regionRect.top,width:regionRect.width,height:regionRect.height},coordinateSystem:'viewport-css-px',layoutHash:stableRef(JSON.stringify(regionRect))}:null);
+    if(observeMode==='overview')return {success:true,mode:'overview',url:location.href,title:document.title,coordinateSystem:'viewport-css-px',viewport:{width:innerWidth,height:innerHeight,devicePixelRatio,scrollX,scrollY},regions:regions(),regionCount:regions().length};
+    const expectedHash=String(a.expectedRegionLayoutHash||''),layoutChanged=!!(expectedHash&&regionData&&expectedHash!==regionData.layoutHash);
+    const relatedPortalIds=new Set();
+    if(regionElement&&a.includePortals!==false)for(const owner of regionElement.querySelectorAll('[aria-controls],[aria-owns]'))for(const id of String(owner.getAttribute('aria-controls')||owner.getAttribute('aria-owns')||'').split(/\s+/))if(id)relatedPortalIds.add(id);
+    const inRelatedPortal=el=>{for(let current=el;current;current=current.parentElement)if(current.id&&relatedPortalIds.has(current.id))return true;return false;};
+    const regionMatch=r=>{if(!regionRect)return {included:true,ratio:1};const hit=intersection(r,regionRect),included=regionMode==='fullyInside'?hit.fullyInside:regionMode==='intersecting'?hit.ratio>0:hit.centerInside;return {included,ratio:hit.ratio};};
     const roots=[{root:document,offsetX:0,offsetY:0}]; const seen=new Set();
     for(let ri=0;ri<roots.length;ri++){
       const context=roots[ri],root=context.root;if(seen.has(root))continue;seen.add(root);
@@ -124,5 +150,6 @@ std::string ObserveScript(const base::DictValue& command) {
         if(el.tagName==='IFRAME'){try{if(el.contentDocument)roots.push({root:el.contentDocument,offsetX:r.left+el.clientLeft,offsetY:r.top+el.clientTop});}catch(_){}}
         if(!visible(el,r))continue;
+        const regionIntersection=regionMatch(r),relatedPortal=inRelatedPortal(el);if(!regionIntersection.included&&!relatedPortal)continue;
         let kind='';const tag=el.tagName.toLowerCase(),computed=style(el),media=mediaInfo(el,computed),clickable=el.matches(interactive)||computed.cursor==='pointer';
         if(a.includeMedia!==false&&media.isMedia)kind='media';
         else if(clickable)kind='interactive';
@@ -143,5 +170,6 @@ std::string ObserveScript(const base::DictValue& command) {
         const locator=selectorInfo(el),container=containerInfo(el),nodeRef=stableRef(locator.selector);
         const item={id:'e'+matched,stableRef:nodeRef,kind,tag,selector:locator.selector,selectorUnique:locator.unique,selectorMatchCount:locator.matchCount,selectorStability:locator.stability,locator:{strategy:semantic.role&&accessible.text?'role-and-name':'css',role:semantic.role,name:clip(accessible.text),cssPath:locator.selector,unique:locator.unique,matchCount:locator.matchCount},text:safeText.slice(0,textLimit),textTruncated:safeText.length>textLimit,name:clip(el.getAttribute('name')),label:clip(accessible.text),labelSource:accessible.source,labelConfidence:accessible.confidence,placeholder:clip(el.getAttribute('placeholder')),ariaLabel:clip(el.getAttribute('aria-label')),value:inputType==='password'?'':clip(el.value),disabled:!!el.disabled,role:semantic.role,controlType:semantic.controlType,editable:semantic.editable,multiline:semantic.multiline,readOnly:!!el.readOnly,required:!!el.required,focused:el===el.ownerDocument.activeElement,visible:true,inViewport:true,occluded:false,...(container?{containerRef:container.stableRef,container,associatedText:container.text}:{}),...(inputType?{inputType}:{}),...(inputType==='file'?{requiresFileUpload:true}:{}),...('checked' in el?{checked:!!el.checked}:{}),...(el.getAttribute('aria-selected')!==null?{selected:el.getAttribute('aria-selected')==='true'}:{}),...(el.getAttribute('aria-checked')!==null?{ariaChecked:el.getAttribute('aria-checked')}:{}),...(el.getAttribute('aria-expanded')!==null?{expanded:el.getAttribute('aria-expanded')==='true'}:{}),...(el.getAttribute('aria-pressed')!==null?{pressed:el.getAttribute('aria-pressed')==='true'}:{}),x:Math.round(r.left),y:Math.round(r.top),width:Math.round(r.width),height:Math.round(r.height)};
+        if(regionData){item.insideRegion=regionIntersection.included;item.intersection={mode:regionMode,ratio:regionIntersection.ratio};item.regionRef=regionData.ref;item.ancestorContext=ancestorContext(el,regionElement);if(relatedPortal)item.relatedPortal=true;}
         if(!accessible.text&&clickable)item.unresolvedReason='interactive control without accessible name';
         if(/删除|移除|清空|关闭/.test(accessible.text)||/delete|remove|trash/.test(String(el.className||'')))item.destructiveHint=true;
         if(tag==='select'){item.multiple=!!el.multiple;item.options=[...el.options].slice(0,50).map(option=>({text:clip(option.text),value:clip(option.value),selected:option.selected,disabled:option.disabled}));item.optionsTruncated=el.options.length>50;}
@@ -157,6 +185,6 @@ std::string ObserveScript(const base::DictValue& command) {
     const isFileUrl=url=>/\.(?:zip|7z|rar|pdf|docx?|xlsx?|pptx?|csv|json|mp4|mp3|wav)(?:[?#]|$)/i.test(String(url||''));
     const downloadLinks=items.filter(i=>i.downloadUrl&&(i.downloadFilename||i.tag==='a'&&isFileUrl(i.downloadUrl))).map(i=>({url:i.downloadUrl,ref:i.id,kind:i.kind,text:i.text||'',selector:i.selector,...(i.downloadFilename?{filename:i.downloadFilename}:{})}));
     const mediaResources=items.filter(i=>i.kind==='media'&&i.mediaUrl).map(i=>({ref:i.id,type:i.mediaType,src:i.mediaUrl,srcset:i.mediaUrls||[],navigationHref:i.linkedUrl||null,downloadable:false}));
-    return {success:true,url:location.href,title:document.title,viewport:{width:innerWidth,height:innerHeight,scrollX,scrollY},items,downloadLinks,downloadLinkCount:downloadLinks.length,mediaResources,mediaResourceCount:mediaResources.length,count:items.length,totalMatched:matched,truncated:matched>items.length,topmostFiltered:true,textLimit,filterDiagnostics:{legacyFilter:kindFilter||null,kinds:kindFilters,tags:tagFilters,roles:roleFilters,controlTypes:controlTypeFilters}};
+    return {success:true,url:location.href,title:document.title,coordinateSystem:'viewport-css-px',viewport:{width:innerWidth,height:innerHeight,devicePixelRatio,scrollX,scrollY},region:regionData?{...regionData,valid:true}:null,regions:a.includeRegions===true?regions():undefined,regionStillValid:true,currentBounds:regionData?.bounds||null,layoutChanged,items,downloadLinks,downloadLinkCount:downloadLinks.length,mediaResources,mediaResourceCount:mediaResources.length,count:items.length,totalMatched:matched,truncated:matched>items.length,topmostFiltered:true,textLimit,filterDiagnostics:{legacyFilter:kindFilter||null,kinds:kindFilters,tags:tagFilters,roles:roleFilters,controlTypes:controlTypeFilters}};
   } catch(e) { return {success:false,error:String(e&&e.message||e),errorCode:'OBSERVE_FAILED'}; } })())JS"});
 }
