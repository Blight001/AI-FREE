diff --git a/chrome/browser/ui/views/frame/ai_free_page_automation.cc b/chrome/browser/ui/views/frame/ai_free_page_automation.cc
--- a/chrome/browser/ui/views/frame/ai_free_page_automation.cc
+++ b/chrome/browser/ui/views/frame/ai_free_page_automation.cc
@@ -138,3 +138,6 @@ std::string ActionScript(const base::DictValue& command) {
     if(mouseAction&&pointRequested){const x=Number(a.x),y=Number(a.y);if(!validPoint(x,y))return {success:false,action,error:'目标坐标超出当前视口',errorCode:'TARGET_COORDINATE_INVALID'};if(action==='drag'){const toX=Number(a.toX),toY=Number(a.toY);if(!validPoint(toX,toY))return {success:false,action,error:'拖拽终点超出当前视口',errorCode:'TARGET_COORDINATE_INVALID'};return {success:true,action,ref:String(a.ref||''),targetMode:'viewport-drag',target:{x,y,toX,toY},nativeDrag:{x,y,toX,toY,viewportWidth:innerWidth,viewportHeight:innerHeight}};}return {success:true,action,ref:String(a.ref||''),targetMode:'viewport-coordinate',target:{x,y},nativeInput:{x,y,viewportWidth:innerWidth,viewportHeight:innerHeight}};}
-    if(action==='wait')return find()?{success:true,action,found:true}:{success:false,action,error:'等待元素中',errorCode:'WAIT_PENDING'};
+    const visible=el=>{if(!el)return false;const r=el.getBoundingClientRect(),c=getComputedStyle(el);return r.width>=1&&r.height>=1&&c.display!=='none'&&c.visibility!=='hidden'&&c.visibility!=='collapse'&&Number(c.opacity)!==0&&!el.hidden;};
+    const elementValue=el=>String(el?.value??el?.innerText??el?.textContent??'').trim().replace(/\s+/g,' ');
+    const waitState=()=>{const condition=String(a.condition||'attached'),el=find(),currentValue=condition==='url_matches'?location.href:elementValue(el),expected=String(a.expectedValue??''),hasInitial=Object.prototype.hasOwnProperty.call(a,'initialValue'),initial=String(a.initialValue??'');const matched=condition==='visible'?visible(el):condition==='hidden'?!visible(el):condition==='text_contains'?!!el&&currentValue.includes(expected):condition==='text_changed'?hasInitial&&!!el&&currentValue!==initial:condition==='url_matches'?currentValue.includes(expected):!!el;return {condition,el,currentValue,matched};};
+    if(action==='wait'){const state=waitState();return state.matched?{success:true,action,condition:state.condition,found:!!state.el,currentValue:state.currentValue,url:location.href}:{success:false,action,condition:state.condition,currentValue:state.currentValue,url:location.href,error:'等待条件中',errorCode:'WAIT_PENDING'};}
     const el=find();if(!el)return {success:false,action,error:'未找到目标元素',errorCode:'ELEMENT_NOT_FOUND'};el.scrollIntoView({block:'center',inline:'center'});el.focus();
