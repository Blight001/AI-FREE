diff --git a/chrome/browser/ui/views/frame/ai_free_page_automation.cc b/chrome/browser/ui/views/frame/ai_free_page_automation.cc
--- a/chrome/browser/ui/views/frame/ai_free_page_automation.cc
+++ b/chrome/browser/ui/views/frame/ai_free_page_automation.cc
@@ -71,8 +71,12 @@ std::string ObserveScript(const base::DictValue& command) {
     const limit=Math.max(1,Math.min(1000,Number(a.limit)||200));
     const textLimit=Math.max(20,Math.min(500,Number(a.textLimit)||120));
     const keyword=String(a.keyword||'').toLowerCase();
-    const tagFilter=String(a.tag||'').toLowerCase();
+    const list=value=>(Array.isArray(value)?value:[]).map(item=>String(item||'').trim().toLowerCase()).filter(Boolean);
+    const tagFilters=[...new Set([...list(a.tags),String(a.tag||'').toLowerCase()].filter(Boolean))];
+    const kindFilters=list(a.kinds),roleFilters=list(a.roles),controlTypeFilters=list(a.controlTypes);
     const kindFilter=String(a.filter||'').toLowerCase();
+    const supportedLegacyFilters=['interactive','media','text','input','form','button','link','checkbox','radio','switch','textbox','searchbox','combobox','listbox','option','slider','spinbutton','tab','menuitem','menuitemcheckbox','menuitemradio','treeitem','select','clickable','text-input','number-input','date-time-input','file-input','color-input','rich-text-input','icon-button','disclosure'];
+    if(kindFilter&&!supportedLegacyFilters.includes(kindFilter))return {success:false,errorCode:'INVALID_FILTER',error:'不支持的 filter；请改用 kinds、tags、roles 或 control_types',filterParsedAs:'unsupported',normalizedFilter:kindFilter,supportedValues:supportedLegacyFilters};
     const interactive='a[href],button,input:not([type="hidden"]),textarea,select,summary,[contenteditable]:not([contenteditable="false"]),[role="button"],[role="link"],[role="checkbox"],[role="radio"],[role="switch"],[role="textbox"],[role="searchbox"],[role="combobox"],[role="listbox"],[role="option"],[role="slider"],[role="spinbutton"],[role="tab"],[role="menuitem"],[role="menuitemcheckbox"],[role="menuitemradio"],[role="treeitem"]';
     const items=[]; let matched=0;
     const isElement=(node)=>!!node&&node.nodeType===1;
@@ -84,7 +88,14 @@ std::string ObserveScript(const base::DictValue& command) {
     const mediaInfo=(el,computed)=>{const tag=el.tagName.toLowerCase(),urls=[];const add=value=>{const url=absoluteUrl(value);if(url&&!urls.includes(url))urls.push(url);};if(tag==='img'){add(el.currentSrc);add(el.src);for(const url of srcsetUrls(el.getAttribute('srcset')))add(url);const picture=el.closest('picture');if(picture){for(const source of picture.querySelectorAll('source'))for(const url of srcsetUrls(source.srcset))add(url);}for(const name of ['data-src','data-original','data-image-url'])add(el.getAttribute(name));}else if(tag==='video'){add(el.currentSrc);add(el.src);add(el.poster);}else if(tag==='audio'){add(el.currentSrc);add(el.src);}for(const url of backgroundUrls(computed.backgroundImage))add(url);return {isMedia:/^(img|video|audio|canvas)$/.test(tag)||urls.length>0,type:/^(img|video|audio|canvas)$/.test(tag)?tag:'background-image',urls};};
     const roleOf=(el)=>String(el.getAttribute('role')||'').trim().toLowerCase();
     const labelOf=(el)=>{const labels=el.labels?[...el.labels]:[];const wrapping=el.closest?.('label');if(wrapping&&!labels.includes(wrapping))labels.push(wrapping);return labels.map(label=>String(label.innerText||label.textContent||'').trim()).filter(Boolean).join(' ').replace(/\s+/g,' ');};
-    const controlInfo=(el,tag,inputType)=>{const explicitRole=roleOf(el);let role=explicitRole,controlType=explicitRole,editable=false,multiline=false;if(tag==='input'){const type=inputType||'text';if(['button','submit','reset','image'].includes(type)){role=role||'button';controlType='button';}else if(type==='checkbox'||type==='radio'||type==='range'){role=role||(type==='range'?'slider':type);controlType=role;}else if(type==='file'||type==='color'){role=role||'button';controlType=type+'-input';}else{role=role||(type==='search'?'searchbox':type==='number'?'spinbutton':'textbox');controlType=type==='number'?'number-input':/^(date|datetime-local|month|time|week)$/.test(type)?'date-time-input':'text-input';editable=true;}}else if(tag==='textarea'){role=role||'textbox';controlType='text-input';editable=true;multiline=true;}else if(tag==='select'){role=role||(el.multiple?'listbox':'combobox');controlType='select';}else if(tag==='button'){role=role||'button';controlType='button';}else if(tag==='a'){role=role||'link';controlType='link';}else if(tag==='summary'){role=role||'button';controlType='disclosure';}else if(el.isContentEditable){role=role||'textbox';controlType='rich-text-input';editable=true;multiline=true;}else if(!controlType){controlType='clickable';}return {role,controlType,editable,multiline};};
-    const matchesFilter=(filter,kind,info)=>!filter||filter===kind||filter===info.role||filter===info.controlType||(filter==='input'&&info.editable)||(filter==='form'&&['input','select','button'].some(value=>info.controlType.includes(value)));
-    const selector=(el)=>{if(el.id)return '#'+CSS.escape(el.id);const n=el.getAttribute('name');if(n)return el.tagName.toLowerCase()+'[name="'+CSS.escape(n)+'"]';let s=el.tagName.toLowerCase();const p=el.parentElement;if(p){const same=[...p.children].filter(x=>x.tagName===el.tagName);if(same.length>1)s+=':nth-of-type('+(same.indexOf(el)+1)+')';}return s;};
+    const referencedLabel=(el)=>String(el.getAttribute('aria-labelledby')||'').split(/\s+/).map(id=>el.ownerDocument.getElementById(id)?.textContent||'').join(' ').trim();
+    const iconHint=(el)=>{const source=[el.getAttribute('class'),el.querySelector('svg use')?.getAttribute('href'),el.querySelector('svg use')?.getAttribute('xlink:href')].filter(Boolean).join(' ').toLowerCase();for(const [pattern,label] of [[/delete|remove|trash|close/,'删除'],[/edit|pencil/,'编辑'],[/more|ellipsis|menu/,'更多'],[/share/,'分享'],[/setting|gear/,'设置'],[/search/,'搜索'],[/add|plus|create/,'新增']])if(pattern.test(source))return label;return '';};
+    const accessibleName=(el,label)=>{for(const [value,source,confidence] of [[el.getAttribute('aria-label'),'aria-label',1],[referencedLabel(el),'aria-labelledby',1],[label,'label',1],[el.getAttribute('title'),'title',.98],[el.querySelector('svg title')?.textContent,'svg-title',.96],[el.getAttribute('alt'),'alt',.95],[iconHint(el),'icon-hint',.72]]){const text=String(value||'').trim().replace(/\s+/g,' ');if(text)return {text,source,confidence};}return {text:'',source:'unresolved',confidence:0};};
+    const controlInfo=(el,tag,inputType,clickable,name)=>{const explicitRole=roleOf(el);let role=explicitRole,controlType=explicitRole,editable=false,multiline=false;if(tag==='input'){const type=inputType||'text';if(['button','submit','reset','image'].includes(type)){role=role||'button';controlType='button';}else if(type==='checkbox'||type==='radio'||type==='range'){role=role||(type==='range'?'slider':type);controlType=role;}else if(type==='file'||type==='color'){role=role||'button';controlType=type+'-input';}else{role=role||(type==='search'?'searchbox':type==='number'?'spinbutton':'textbox');controlType=type==='number'?'number-input':/^(date|datetime-local|month|time|week)$/.test(type)?'date-time-input':'text-input';editable=true;}}else if(tag==='textarea'){role=role||'textbox';controlType='text-input';editable=true;multiline=true;}else if(tag==='select'){role=role||(el.multiple?'listbox':'combobox');controlType='select';}else if(tag==='button'){role=role||'button';controlType=name.text&&!String(el.innerText||'').trim()?'icon-button':'button';}else if(tag==='a'){role=role||'link';controlType='link';}else if(tag==='summary'){role=role||'button';controlType='disclosure';}else if(el.isContentEditable){role=role||'textbox';controlType='rich-text-input';editable=true;multiline=true;}else if(clickable){role=role||'button';controlType=name.text&&!String(el.innerText||'').trim()?'icon-button':'clickable';}return {role,controlType,editable,multiline};};
+    const legacyMatch=(filter,kind,info)=>!filter||filter===kind||filter===info.role||filter===info.controlType||(filter==='input'&&info.editable)||(filter==='form'&&['input','select','button'].some(value=>info.controlType.includes(value)));
+    const matchesFilter=(kind,info,tag)=>legacyMatch(kindFilter,kind,info)&&(!kindFilters.length||kindFilters.includes(kind))&&(!tagFilters.length||tagFilters.includes(tag))&&(!roleFilters.length||roleFilters.includes(info.role))&&(!controlTypeFilters.length||controlTypeFilters.includes(info.controlType));
+    const localSelector=(el)=>{if(el.id)return '#'+CSS.escape(el.id);const n=el.getAttribute('name');if(n)return el.tagName.toLowerCase()+'[name="'+CSS.escape(n)+'"]';let s=el.tagName.toLowerCase();const p=el.parentElement;if(p){const same=[...p.children].filter(x=>x.tagName===el.tagName);if(same.length>1)s+=':nth-of-type('+(same.indexOf(el)+1)+')';}return s;};
+    const selectorInfo=(el)=>{let current=el,parts=[];for(let depth=0;current&&depth<8;depth++,current=current.parentElement){parts.unshift(localSelector(current));const candidate=parts.join(' > ');let count=0;try{count=el.ownerDocument.querySelectorAll(candidate).length;}catch(_){}if(count===1)return {selector:candidate,unique:true,matchCount:1,stability:candidate.startsWith('#')?'high':'medium'};}const selector=parts.join(' > ')||localSelector(el);let count=0;try{count=el.ownerDocument.querySelectorAll(selector).length;}catch(_){}return {selector,unique:count===1,matchCount:count,stability:'low'};};
+    const stableRef=(value)=>{let hash=2166136261;for(const char of value){hash^=char.charCodeAt(0);hash=Math.imul(hash,16777619);}return 'node-'+(hash>>>0).toString(16).padStart(8,'0');};
+    const containerInfo=(el)=>{const node=el.closest?.('[role="listitem"],li,article,tr,form,[data-testid]');if(!node||node===el)return null;const locator=selectorInfo(node);return {stableRef:stableRef(locator.selector),role:roleOf(node)||({LI:'listitem',TR:'row',FORM:'form',ARTICLE:'article'}[node.tagName]||'group'),selector:locator.selector,text:String(node.innerText||node.textContent||'').trim().replace(/\s+/g,' ').slice(0,textLimit)};};
     const composedParent=(el)=>{if(el.parentElement)return el.parentElement;const root=el.getRootNode?.();return root&&root.host||null;};
@@ -109,14 +123,17 @@ std::string ObserveScript(const base::DictValue& command) {
         if(!kind)continue;
         const point=exposedPoint(el,r,clickable,kind==='media');if(!point)continue;
         const inputType=tag==='input'?String(el.type||'').toLowerCase():'';
-        const semantic=controlInfo(el,tag,inputType);
         const rawText=String(el.innerText||el.textContent||el.value||el.getAttribute('aria-label')||'').trim().replace(/\s+/g,' ');
         const safeText=inputType==='password'?'':rawText;
-        const label=labelOf(el);const searchable=[safeText,label,el.getAttribute('placeholder'),el.getAttribute('aria-label')].filter(Boolean).join(' ').toLowerCase();
-        if(keyword&&!searchable.includes(keyword))continue;if(tagFilter&&tag!==tagFilter)continue;if(!matchesFilter(kindFilter,kind,semantic))continue;
+        const formLabel=labelOf(el),accessible=accessibleName(el,formLabel),semantic=controlInfo(el,tag,inputType,clickable,accessible);
+        const searchable=[safeText,accessible.text,el.getAttribute('placeholder'),el.getAttribute('aria-label')].filter(Boolean).join(' ').toLowerCase();
+        if(keyword&&!searchable.includes(keyword))continue;if(!matchesFilter(kind,semantic,tag))continue;
         matched++;if(items.length>=limit)continue;
         const clip=(value,max=textLimit)=>String(value||'').slice(0,max);
-        const item={id:'e'+matched,kind,tag,selector:selector(el),text:safeText.slice(0,textLimit),textTruncated:safeText.length>textLimit,name:clip(el.getAttribute('name')),label:clip(label),placeholder:clip(el.getAttribute('placeholder')),ariaLabel:clip(el.getAttribute('aria-label')),value:inputType==='password'?'':clip(el.value),disabled:!!el.disabled,role:semantic.role,controlType:semantic.controlType,editable:semantic.editable,multiline:semantic.multiline,readOnly:!!el.readOnly,required:!!el.required,...(inputType?{inputType}:{}),...(inputType==='file'?{requiresFileUpload:true}:{}),...('checked' in el?{checked:!!el.checked}:{}),...(el.getAttribute('aria-checked')!==null?{ariaChecked:el.getAttribute('aria-checked')}:{}),...(el.getAttribute('aria-expanded')!==null?{expanded:el.getAttribute('aria-expanded')==='true'}:{}),...(el.getAttribute('aria-pressed')!==null?{pressed:el.getAttribute('aria-pressed')==='true'}:{}),x:Math.round(r.left),y:Math.round(r.top),width:Math.round(r.width),height:Math.round(r.height)};
+        const locator=selectorInfo(el),container=containerInfo(el),nodeRef=stableRef(locator.selector);
+        const item={id:'e'+matched,stableRef:nodeRef,kind,tag,selector:locator.selector,selectorUnique:locator.unique,selectorMatchCount:locator.matchCount,selectorStability:locator.stability,locator:{strategy:semantic.role&&accessible.text?'role-and-name':'css',role:semantic.role,name:clip(accessible.text),cssPath:locator.selector,unique:locator.unique,matchCount:locator.matchCount},text:safeText.slice(0,textLimit),textTruncated:safeText.length>textLimit,name:clip(el.getAttribute('name')),label:clip(accessible.text),labelSource:accessible.source,labelConfidence:accessible.confidence,placeholder:clip(el.getAttribute('placeholder')),ariaLabel:clip(el.getAttribute('aria-label')),value:inputType==='password'?'':clip(el.value),disabled:!!el.disabled,role:semantic.role,controlType:semantic.controlType,editable:semantic.editable,multiline:semantic.multiline,readOnly:!!el.readOnly,required:!!el.required,focused:el===el.ownerDocument.activeElement,visible:true,inViewport:true,occluded:false,...(container?{containerRef:container.stableRef,container,associatedText:container.text}:{}),...(inputType?{inputType}:{}),...(inputType==='file'?{requiresFileUpload:true}:{}),...('checked' in el?{checked:!!el.checked}:{}),...(el.getAttribute('aria-selected')!==null?{selected:el.getAttribute('aria-selected')==='true'}:{}),...(el.getAttribute('aria-checked')!==null?{ariaChecked:el.getAttribute('aria-checked')}:{}),...(el.getAttribute('aria-expanded')!==null?{expanded:el.getAttribute('aria-expanded')==='true'}:{}),...(el.getAttribute('aria-pressed')!==null?{pressed:el.getAttribute('aria-pressed')==='true'}:{}),x:Math.round(r.left),y:Math.round(r.top),width:Math.round(r.width),height:Math.round(r.height)};
+        if(!accessible.text&&clickable)item.unresolvedReason='interactive control without accessible name';
+        if(/删除|移除|清空|关闭/.test(accessible.text)||/delete|remove|trash/.test(String(el.className||'')))item.destructiveHint=true;
         if(tag==='select'){item.multiple=!!el.multiple;item.options=[...el.options].slice(0,50).map(option=>({text:clip(option.text),value:clip(option.value),selected:option.selected,disabled:option.disabled}));item.optionsTruncated=el.options.length>50;}
         if(tag==='button')item.buttonType=String(el.type||'submit').toLowerCase();
         if(clickable){item.interactive=true;item.clickX=Math.round(point.x*100)/100;item.clickY=Math.round(point.y*100)/100;}
@@ -126,7 +146,9 @@ std::string ObserveScript(const base::DictValue& command) {
         if(tag==='a'&&/^https?:/i.test(el.href||'')){item.href=String(el.href).slice(0,2048);item.downloadUrl=item.href;const n=el.getAttribute('download');if(n)item.downloadFilename=String(n).slice(0,160);}items.push(item);
       }
     }
-    const downloadLinks=items.filter(i=>i.downloadUrl).map(i=>({url:i.downloadUrl,ref:i.id,kind:i.kind,text:i.text||'',selector:i.selector,...(i.mediaType?{mediaType:i.mediaType}:{}),...(i.linkedUrl?{linkedUrl:i.linkedUrl}:{}),...(i.downloadFilename?{filename:i.downloadFilename}:{})}));
-    return {success:true,url:location.href,title:document.title,viewport:{width:innerWidth,height:innerHeight,scrollX,scrollY},items,downloadLinks,downloadLinkCount:downloadLinks.length,count:items.length,totalMatched:matched,truncated:matched>items.length,topmostFiltered:true,textLimit};
+    const isFileUrl=url=>/\.(?:zip|7z|rar|pdf|docx?|xlsx?|pptx?|csv|json|mp4|mp3|wav)(?:[?#]|$)/i.test(String(url||''));
+    const downloadLinks=items.filter(i=>i.downloadUrl&&(i.downloadFilename||i.tag==='a'&&isFileUrl(i.downloadUrl))).map(i=>({url:i.downloadUrl,ref:i.id,kind:i.kind,text:i.text||'',selector:i.selector,...(i.downloadFilename?{filename:i.downloadFilename}:{})}));
+    const mediaResources=items.filter(i=>i.kind==='media'&&i.mediaUrl).map(i=>({ref:i.id,type:i.mediaType,src:i.mediaUrl,srcset:i.mediaUrls||[],navigationHref:i.linkedUrl||null,downloadable:false}));
+    return {success:true,url:location.href,title:document.title,viewport:{width:innerWidth,height:innerHeight,scrollX,scrollY},items,downloadLinks,downloadLinkCount:downloadLinks.length,mediaResources,mediaResourceCount:mediaResources.length,count:items.length,totalMatched:matched,truncated:matched>items.length,topmostFiltered:true,textLimit,filterDiagnostics:{legacyFilter:kindFilter||null,kinds:kindFilters,tags:tagFilters,roles:roleFilters,controlTypes:controlTypeFilters}};
   } catch(e) { return {success:false,error:String(e&&e.message||e),errorCode:'OBSERVE_FAILED'}; } })())JS"});
 }
